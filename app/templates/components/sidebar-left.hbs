<div class="sidebar-index">
  {{#paper-list}}
    {{#paper-subheader}}Navigation{{/paper-subheader}}
    {{#each category as |cat|}}
      {{#paper-item
        class="md-body-2"
        onClick=(action "redirectToLink" cat.title)
      }}
        <div class="md-list-item-text">
          <div>
            # {{cat.title}}
          </div>
        </div>
      {{/paper-item}}
    {{/each}}
    {{paper-divider}}
  {{/paper-list}}
</div>