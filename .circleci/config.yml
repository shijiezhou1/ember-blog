version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - run:
          name: Checking Node and Npm Versions
          command: |
            node --version
            npm --version
            npm install
            npm run build
